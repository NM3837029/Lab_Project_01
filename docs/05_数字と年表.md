# 数字と年表

作品名は **Editron**。`Lab Project 01` はリポジトリと実行ファイルの名前です。

**この紙の数字は全部、下に書いたコマンドで引き直せます。**
出展の直前にもう一度流して、ずれていたら書き換えてください
（最終確認：2026年9月17日）。

---

## コードの規模

| 項目 | 値 |
|---|---|
| 自作コード合計 | **約 32,000 行** |
| ゲーム本体（C++） | **13,216 行** |
| └ `DrawPixel.cpp` | **11,030 行**（765 KB・ゲームループと全ロジック） |
| └ `BehaviorScript.h` | 669 行（ブロックスクリプトの実行エンジン） |
| └ `GameConfig.h` | 515 行（タイトル画面・セーブ・効果音の割り当て） |
| └ `EventManager.h` | 318 行（トリガーイベント） |
| └ `AnimationController.h` | 232 行（スプライトアニメーション） |
| └ `SoundManager.h` | 225 行（BGM / SE） |
| └ `Logger.h` | 132 行 |
| └ `GamePaths.h` | 95 行（配布時のパス解決） |
| Lab_Editor（C#） | **48 ファイル / 18,796 行**（手書き分のみ。`obj/` `bin/` を除く） |
| └ 最大ファイル | `PartsEditorPageControl.cs` 2,047 行 |
| C++ の `struct` | 55（本体 35 ＋ ヘッダ 20） |
| C++ の `enum` | 10 |
| C# の class / enum / struct | 98 |

> **外部ライブラリは数えていません。** DxLib・Dear ImGui・nlohmann-json は同梱していますが、
> どれも自分で書いたものではないので上の行数には含めていません。

---

## 中身の規模

| 項目 | 値 |
|---|---|
| 敵の行動タイプ | **23種**（うち1種はカスタムスクリプト） |
| ギミックの型 | **26種**（うち1種はカスタムスクリプト） |
| ブロック命令 | **73種**（数値レポーター30／真偽16／実行24／開始ブロック3） |
| 敵の定義 | 23 |
| ギミックの定義 | **36**（26種類の型に分かれている＝同じ型から別物を作っている） |
| アイテムの定義 | 13 |
| タイルの定義 | 14 |
| 複合パーツ | **19アセットに合計59パーツ** |
| BGM / SE / UI音 | 5 / 16 / 9 |
| 編集ツール | 6カテゴリ（ステージごとにON/OFF可） |

### ステージ

| ステージ | マス | ピクセル | 敵 | ギミック | アイテム |
|---|---|---|---|---|---|
| はじまりの草原 | 80×15 | 2560×480 | 4 | 1 | 15 |
| 編集のとびら | 120×15 | 3840×480 | 2 | 17 | 17 |
| たての塔（縦） | 20×64 | 640×2048 | 1 | 21 | 15 |
| いろの回廊 | 100×15 | 3200×480 | 1 | 17 | 13 |
| かたむく丘 | 96×15 | 3072×480 | 6 | 1 | 17 |
| ほりぬきの坑道 | 110×20 | 3520×640 | 3 | 11 | 18 |
| わきあがる縦坑（縦） | 24×72 | 768×2304 | 5 | 8 | 24 |
| **合計 7本** | | | **22** | **76** | **119** |

タイルは1マス32px。各ステージは**3層**（遠景・メイン・近景）のタイルレイヤを持ちます。

### 素材

| 項目 | 値 |
|---|---|
| 画像 | PNG **57枚**（スプライト50枚は全て 640×640。ほかステージ選択のサムネイル7枚）＋ 構成図の SVG 1枚 |
| BGM | 6 ファイル（WAV） |
| 効果音 | 21 ファイル（WAV） |

---

## 動作環境とビルド

| 項目 | 値 |
|---|---|
| 画面 | 内部 640×480 で描き、1280×720 のウィンドウへ拡大。60fps |
| ゲーム本体 | C++17 / MSVC v143（Visual Studio 2022）/ x64 |
| 文字コード | `/execution-charset:utf-8`（日本語ファイル名の画像を読むため） |
| DxLib | Ver 3.24f（同梱） |
| Dear ImGui | 1.92.9（ゲーム内デバッグUI用） |
| nlohmann-json | 3.11.2（単一ヘッダ） |
| Lab_Editor | .NET 8 / WinForms |
| Lab_Editor の外部依存 | **Newtonsoft.Json 13.0.4 の1つだけ** |
| 配布 | zip を展開して exe をダブルクリック。セーブは `%LOCALAPPDATA%\LabProject01\` |

---

## 年表（git のコミット履歴から）

**2026年5月11日 〜 9月17日、約4ヶ月。コミット73本。3人。**

| 時期 | できたもの |
|---|---|
| 5月 | ゲーム本体に着手。敵、重量スイッチ、ちくわブロック |
| 6月 | ゲームシーンの整理、反・一時停止装置 |
| **7月6日** | **Lab_Editor に着手**（ここから開発の速度が変わる） |
| 7月 | ステージ生成まわりの改良 |
| **7月16日** | **複合パーツ・システム**（1体を複数画像で組み立てる） |
| 8月 | 縦スクロール対応、タイル関連、サウンド管理、**編集コストゲージ** |
| 8月27日 | カット機能の復元とチェックポイント |
| 8月31日 | 画像素材の入れ替え |
| **9月8日** | **全アセットの作り直し**（新素材41枚に合わせて定義を全部組み直し）、カットコストの距離変動 |
| **9月9日** | **編集リアクション**（全編集操作 × 全敵・全ギミックに固有の反応） |
| **9月10日** | **配布(zip)対応・タイトル画面・ステージ選択・セーブ** |
| 9月13日 | 複合パーツを親の拡大・傾けに連動 |
| 9月14日 | 敵の落下・突進の向きを編集した姿勢に連動。効果音の整理 |
| 9月15日 | 地形タイルの破壊、配置ごとのサイズ・角度 |
| 9月16日 | **トリッキーなステージ3本**（地上・地下・縦）、機械いもむし |
| 9月17日 | 砲台・ブロックのバグ修正、配布の入口を Lab_Editor に追加 |

**「7月6日にエディタを作り始めた」のが転換点**として話すと伝わります。
それ以前はステージもアセットも JSON を手で書いていました。

---

## 数え直すコマンド

プロジェクトのルート（`Lab_Project_01.vcxproj` があるフォルダ）で実行します。
Git Bash / WSL 想定です。

### コード行数

```bash
# ゲーム本体（C++）。json.hpp は外部ライブラリなので除く
wc -l DrawPixel.cpp BehaviorScript.h GameConfig.h EventManager.h \
      AnimationController.h SoundManager.h Logger.h GamePaths.h

# Lab_Editor（C#）。ビルド生成物を除いた手書き分だけ
find Lab_Editor -name "*.cs" -not -path "*/obj/*" -not -path "*/bin/*" | wc -l
find Lab_Editor -name "*.cs" -not -path "*/obj/*" -not -path "*/bin/*" \
  -print0 | xargs -0 wc -l | tail -1
```

### 型の数

```bash
H="BehaviorScript.h EventManager.h GameConfig.h AnimationController.h \
   SoundManager.h Logger.h GamePaths.h"
grep -hcE '^[[:space:]]*struct [A-Za-z_]+' DrawPixel.cpp $H   # 足し算する
grep -hcE '^[[:space:]]*enum '             DrawPixel.cpp $H
```

### 敵・ギミックの型の数

```bash
# 敵（末尾の番兵 ENEMY_TYPE_COUNT は数に入れない）
sed -n '/^enum EnemyType/,/^};/p' DrawPixel.cpp \
  | grep -oE '^\s+ENEMY_[A-Z_0-9]+' | grep -v TYPE_COUNT | wc -l

# ギミック
sed -n '/^enum GimmickType/,/^};/p' DrawPixel.cpp \
  | grep -cE '^\s+GIMMICK_[A-Z_0-9]+'
```

### ブロック命令の数

```bash
grep -c "new BlockDef(" Lab_Editor/Lab_Editor/BlockDef.cs
```

### アセットの定義数

```bash
for f in enemies gimmicks items tiles; do
  printf "%-10s " "$f"; grep -cE '^    "id"' assets/$f.json
done
```

### 素材の数

```bash
ls img/*.png | wc -l          # うち thumb_*.png 7枚はステージ選択のサムネイル
ls img/thumb_*.png | wc -l
ls sound/ | wc -l
ls se/    | wc -l
```

### ステージの中身

```bash
python -c "
import json,io,glob,os
for p in sorted(glob.glob('assets/stages/*.json')):
    d=json.load(io.open(p,encoding='utf-8'))
    print(os.path.basename(p),
          '%sx%s' % (d.get('map_w'), d.get('map_h')),
          'enemy=%d gimmick=%d item=%d' % (len(d.get('enemies',[])),
                                           len(d.get('gimmicks',[])),
                                           len(d.get('items',[]))))
"
```

### 開発期間とコミット数

```bash
git rev-list --count HEAD
git log --reverse --format="%ad" --date=short | head -1   # 最初のコミット
git log -1 --format="%ad" --date=short                    # 最新のコミット
git shortlog -sn                                          # 人ごとのコミット数
```
